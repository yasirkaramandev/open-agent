# Signal web console

Interactive Next.js/Vinext dashboard for the safety-first YouTube operations example.

```bash
npm install
npm run dev
```

`npm test` performs a production server-render and verifies that the approval flow remains bound to
one reviewed account and payload. The interface uses realistic sample data; it does not contact
Google or execute YouTube writes.
